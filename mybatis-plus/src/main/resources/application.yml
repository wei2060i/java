spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chen
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver


mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bean
  global-config:
    db-config:
      id-type: auto
      table-prefix: tbl_


logging:
  level:
    com.mapper: debug
